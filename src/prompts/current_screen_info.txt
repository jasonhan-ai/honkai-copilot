请详细描述这个界面截图中的内容，包括界面布局、文本内容和视觉元素,返回各个元素的绝对像素位置。请用中文回答。格式类似如下：
## 界面布局
- **主场景**：游戏中的室内场景，包含以下元素：
  - **家具**: 
    - 靠近入口的红色长椅（位于屏幕右侧，约(1400, 600)）。
    - 两把红色单人椅和一个小茶几（位于左侧，约(300, 500)）。
    - 另一套对称的红色单人椅和茶几（位于中心偏后，约(500, 550)）。
  - **植物**: 多个绿色植物摆放在窗台和区域隔板上（约(800, 300)）。
  - **人物**: 
    - 主角（灰发，黑色衣服，站在中心，约(950, 800)）。
    - 另一个角色坐在远处的红色长椅上（约(1300, 600)）。

- **UI元素**：
  - **左上角**：
    - **观景车厢**（约(20, 20)）。
    - **灯泡图标**（约(100, 20)）。
  - **左中下**：
    - **破碎的记忆**（约(80, 300)，文字描述）。
    - **任务提示**："回溯记忆，寻找线索 0/3"（约(80, 350)）。
    - **指引提示**："请前往镭法洛斯-‘命运重奏’雅努萨波利斯"（约(80, 400)）。
  - **右上角**：
    - **关闭按钮**（约(1830, 20)）。
    - **设置/菜单按钮**（约(1760, 20)）。
    - **其他功能按钮**（约(1600, 20)）。

- **其他元素**：
  - **底部左下角**：
    - **UID: 100071772**（约(50, 980)）。
    - **Enter**（约(80, 980)）。
  - **右下角**：
    - **移动/操作按钮**（约(1800, 900)）。

### 文本内容
- **破碎的记忆**（左中部，约(80, 300)）。
- **回溯记忆，寻找线索 0/3**（左中部，约(80, 350)）。
- **请前往镭法洛斯-“命运重奏”雅努萨波利斯**（左中部，约(80, 400)）。
- **UID: 100071772**（左下角，约(50, 980)）。
- **Enter**（左下角，约(80, 980)）。

### 视觉元素
- **主角**（灰发，黑色衣服，中心，约(950, 800)）。
- **红色家具**：多把红色椅子和长椅，分布在场景中（例如(300, 500), (1400, 600)）。
- **绿色植物**：分布在窗台和区域隔板上（约(800, 300)）。
- **灯光**：场景中有多盏壁灯（约(400, 100), (600, 100)）。
- **背景**：城市夜景从大窗户可见（占据大部分顶部区域）。

